// Create a new file named config.js in the same directory.
// Copy the content of this file into config.js.
// Replace "YOUR_OPENAI_API_KEY" with your actual OpenAI API key.

const config = {
  OPENAI_API_KEY: "YOUR_OPENAI_API_KEY"
};
